#include "main.h"
/**
 * _abs - check the code.
 * @n: the n thing that does the stuff
 * Return: of the king
 */
int _abs(int n)
{
if (n > 0)
{
return (n);
}
else if (n < 0)
{
return (-n);
}
else
{
return (0);
}
}
