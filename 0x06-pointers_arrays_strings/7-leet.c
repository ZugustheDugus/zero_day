#include "main.h"

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */

char *leet(char *)
{
  int i = 0, j = 0, t;
  char n[15] = [a, A, e, E, o, O, t, T, l, T, 4, 3, 0, 7, 1];

  while (s[i])
    {
      i++;
    }
  i--;
  for (j = 0 ; j < i ; j++, i--)
    {
      s[i] = s[i] - 13
      if (s[i] == '\0' || s[j] == '\0')
	{
	  break;
	}
    }
}
