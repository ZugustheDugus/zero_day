#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */

char *_strchr(char *s, char c)
{
  
unsigned int i = 0;
while (s[i])
{
if (s[i] == c)
{
return (s + i);
}
i++; 
}
return (0);
}
